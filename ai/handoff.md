# CODA HANDOFF — single source of truth for any AI agent continuing this project.

RULE: after completing ANY task in this repo, update this file (state, log) before
finishing the session. Keep it factual and current.

## Meta
- updated: 2026-09-24
- repo: github.com/haziqcodes/coda-play (remote to be set once key is added)
- branch: main
- live: local http://127.0.0.1:8000 (this workspace); deploy target: Render free plan (not yet connected)
- stack: Python 3 + Flask + yt-dlp + ffmpeg (all free); single-page static frontend (no build step)

## How to work (Coda)
1. Read /home/user/SKILL.md FIRST — user's permanent rules live there (Hinglish replies, premium quality, zero emojis, English-only UI, light theme only, free-only).
2. Read this page (rendered at /ai, hidden, noindex) for project state.
3. After your task: update ai/handoff.md + SKILL.md log, run `make test` + `make lint`, curl-smoke key routes, commit, push.
4. Commit identity: Mohd Haziq <mohdhaziq-work@users.noreply.github.com>. SSH key /home/user/.ssh/id_ed25519 (see /home/user/.ssh/config).
5. ALWAYS check before pushing: remote exists, git identity set, `chmod 600 /home/user/.ssh/id_ed25519` (workspace snapshots can reset perms).

## User rules that apply here
- Replies to user in Hinglish; website content English only.
- NO emojis anywhere on the website — inline SVG icons only (static/index.html + ICONS in static/app.js).
- Light theme ONLY — monochrome + single blue #1a73e8, Inter + JetBrains Mono (static/style.css :root).
- Everything free (pip + Render free). No paid services, no Vercel for this project.
- Verify before claiming done: pytest + flake8 + curl smoke of /, /api/health, /api/playlist, /ai, /audio/<id>.mp3 (range 206), /thumbs/<id>.jpg.
- Personal-use legal policy: no redistribution features, ever.

## Architecture
- server.py — Flask app: POST /api/add (URL -> background job), GET /api/job/<id> (poll), GET/POST /api/playlist (rename/reorder), POST /api/playlist/tracks/remove, GET /audio/<id>.mp3 (range streaming), GET /thumbs/<id>.jpg, GET /ai (this handoff, noindex).
- static/index.html + static/app.js + static/style.css — single page: add-URL card, playlist list (drag & drop reorder), fixed player bar.
- Core playback rule: exactly ONE <audio> element; next track starts only on the `ended` event (no overlap possible). Repeat modes: off / all / one (R key or button).
- data/ (git-ignored): playlist.json, tracks/*.mp3 (192 kbps), thumbs/*.jpg.
- tests/test_api.py — 8 pytest tests against Flask test client (temp data dir via monkeypatch).
- .github/ — CI (flake8 + pytest on Python 3.11/3.12/3.13), Dependabot (pip + actions), issue/PR templates, CONTRIBUTING, CoC.
- Makefile (install/run/test/lint/docker), Dockerfile (python:3.12-slim + apt ffmpeg), docker-compose.yml.

## Current state
- LIVE: https://coda-play.onrender.com (Render free, Docker, blueprint from render.yaml; region now frankfurt — see log). CI green. main @ latest (see log).
- YouTube extraction from Render is currently IP-flagged by YouTube (bot-check on all clients even with valid cookies). Working architecture: cookie upload (Settings) + config ladder in extract_audio. Reliable fallbacks: run locally (residential IP — verified) or Oracle free VPS. See newest log entry for the full diagnosis.
- V1 COMPLETE 2026-09-24: full pipeline verified end-to-end in this workspace (URL -> yt-dlp -> ffmpeg mp3 -> playlist -> range streaming 206). Test track: Big Buck Bunny (CC-BY, Blender, 635s), re-verified 2026-09-24 after snapshot reset (id 4c149133711a).
- GIT: remote origin = git@github.com:haziqcodes/coda-play.git, key "coda-play-agent" (SHA256:xv/aTTO1iSL+eqt/s5XcnwtRJIzC+duCbg9NLkAGlAU) on that account, commits as Mohd Haziq <haziqcodes@users.noreply.github.com>.
- SNAPSHOT-RESET RECOVERY CHECKLIST (this workspace resets often): pip install flask yt-dlp flake8 pytest -> chmod 700 /home/user/.ssh && chmod 600 id_ed25519 config -> `git remote get-url origin || git remote add origin git@github.com:haziqcodes/coda-play.git` -> re-download ffmpeg (johnvansickle static build -> /home/user/ffmpeg-static, symlinks in /home/user/bin) only if local E2E needed -> rm ffmpeg-static + bin + /tmp/ck*.txt after testing (user's cleanup order).

## Log (newest first)
- 2026-09-25 — RENDER OOM RESTARTS (user: playlist stuck at 10/19, "no supported source", list empty after refresh). ROOT CAUSE measured locally: peak RAM ~530 MB during a download (bgutil PO HTTP server ~150-180 MB idle + deno EJS solver ~240 MB + python ~100 MB) > Render free 512 MB -> OOM kill -> restart -> ephemeral disk wiped (library json + audio gone; in-memory job gone -> UI polled forever). FIXES: (1) PO HTTP server now opt-in (CODA_POT_SERVER=1); idle 34 MB, peak ~350 MB. DOWNLOAD_LOCK semaphore = one yt-dlp at a time. (2) Browser-driven playlist import: GET /api/list?url= (flat list), then browser POSTs /api/add {url, single:true} per song; runJob() retries on cold start, 404 job = "restart" -> retry song (3 tries). Stop = client flag. (3) On-device library: localStorage coda.pl.<CODE> mirror + IndexedDB "coda"/"files" (a:<id> audio blob, t:<id> cover); playIndex plays device blob first; server file 404 -> refetchAndPlay() re-downloads under SAME id (/api/add track_id) and plays. Server returns boot id (BOOT_ID) in /api/playlist; browser stores coda.boot.<CODE>; on boot change it POSTs /api/playlist/restore (merge by id, browser tracks first). "on device" badge per track. (4) PRE-EXISTING BUG fixed: updatePlayerUi referenced missing #pbThumbFallback -> threw on every call (play/pause icon never updated); now null-safe + player cover from device. Verified: pytest 15/15, flake8 clean, Playwright E2E: playlist import survives server kill+disk wipe mid-import; reload after another wipe -> list restored to server (4/4) and plays from blob; linked 2nd browser w/o device copy auto-refetches + plays in 10 s.
- 2026-09-25 — Cookie ladder now cookies+tv first (Render: tv succeeds first try; mweb wasted ~25 s). LIVE measured: playlist ~30 s/track (was 80-160 s). YT_COOKIES_B64 env var confirmed working (health cookies:true after redeploy). Test tracks removed from live.
- 2026-09-25 — SPEED: (a) bgutil PO-token HTTP server (deno run -A src/main.ts, :4416) auto-started from server.py __main__ (warm + token cache) — yt-dlp plugin prefers HTTP, script mode stays as fallback; disable with CODA_NO_POT_SERVER=1. (b) No more mp3 re-encode: format bestaudio[ext=m4a] + FFmpegExtractAudio preferredcodec m4a (stream copy); AUDIO_CODEC=mp3 env restores old behaviour. Track "file" now carries real ext (.m4a), /audio/<id>.<ext> serves any AUDIO_EXTS with correct mime + range; frontend uses t.file (old .mp3 tracks still play). Playlist Stop button (POST /api/job/<id>/cancel, stops after current track) + live failures list in job. Measured before on Render: ~80-160 s/track (script-mode deno per attempt + mp3 encode on 0.1 CPU). Local after: 11 s/track.
- 2026-09-25 — PLAYLISTS + PER-BROWSER LIBRARIES. (1) Playlist URLs (list=…, not RD mixes) are flat-listed first (extract_flat, cap MAX_PLAYLIST env, default 100), then downloaded one by one via download_one(); job has kind=playlist, current/total/added/failed, stage "Track i/N · …", UI refreshes playlist as each track lands and shows overall progress. Before: whole playlist downloaded silently under one job into the same filename -> stuck on "converting to mp3". (2) Libraries: browser generates an 8-char code (localStorage coda.library), sent as X-Coda-Library on every playlist/add/job call; server stores data/libraries/<CODE>.json (no header -> legacy data/playlist.json, used by tests). New "Library" card: shows code, Copy, "Use code" (link another browser), "New library". Browsers cannot share storage and there is no reliable cross-browser device ID, so linking by code is the honest design. Tracks/thumbs remain global files. Tests 12/12 (+library isolation, +playlist URL detection), flake8 clean, local E2E 3-track playlist OK.
- 2026-09-25 — COOKIES WORK ON RENDER: user exported cookies on Android (Firefox + Cookie-Editor, Netscape export). Kept 23 auth cookies (dropped ST-* tracking), uploaded via POST /api/cookies -> IltsOcCj1Ak extracted on LIVE Render (cookies+mweb+PO token), mp3 range 206 OK. Bug fixed: cookie counter treated #HttpOnly_ lines as comments (showed 9 instead of 23). Base64 for Render env var YT_COOKIES_B64 is in /home/user/YT_COOKIES_B64.txt (NOT in repo). Push only AFTER user sets YT_COOKIES_B64 on Render — each deploy wipes data/cookies.txt.
- 2026-09-25 — BLUEPRINT "Failed sync" root cause: Render region is immutable after creation. Service was created in singapore (5f9c73c); commits 99207b4 (frankfurt) and edb2d59 (sydney) made every blueprint sync fail, so the region NEVER actually changed — egress IP stayed the same flagged singapore IP. render.yaml reset to singapore (45ef05e). Real new IP = delete service + recreate from blueprint in another region (user action; re-enter env vars). Live test after deploy: bot-check still blocks IltsOcCj1Ak on Render even with Deno + PO tokens; next = incognito cookies via YT_COOKIES_B64, or run locally / Oracle VPS.
- 2026-09-25 — SSH key rotated: new key coda-play-agent (SHA256:xv/aTTO1iSL+eqt/s5XcnwtRJIzC+duCbg9NLkAGlAU) added by user; SKILL.md recreated after reset.
- 2026-09-25 — BOT-CHECK FIX: root cause was missing JS runtime + PO tokens (current yt-dlp needs both on server IPs; cookies/region hops alone were not enough). Dockerfile now installs Deno (denoland/deno:bin) + bgutil-ytdlp-pot-provider 2.0.0 server at /opt/bgutil/server (script mode, no sidecar); requirements add yt-dlp[default] + bgutil plugin. extract_audio: js_runtimes=deno, remote_components ejs:github, bgutil server_home, ladder mweb/web_safari (PO token) -> default -> android_vr/tv; cookies ladder cookies+mweb -> cookies+tv -> mweb. New env: YTDLP_PROXY, YT_COOKIES_B64 (cookies survive redeploy), BGUTIL_SERVER_HOME. /api/health reports deno/po_token_provider/proxy/cookies. Verified locally: IltsOcCj1Ak -> mp3 via mweb+PO token; pytest 10/10; flake8 clean.
- 2026-09-24 — YouTube "Requested format is not available" + datacenter-IP escalation (LIVE: https://coda-play.onrender.com). User pasted raw browser cookie string (Name=Value; ...), converted to Netscape (html.unescape for &amp;, `.youtube.com TRUE / TRUE 0` lines), uploaded to live Render via POST /api/cookies (22 cookies) — deploy wipes the ephemeral disk, so cookies must be re-uploaded after every push. Root-caused the format error by probing format lists from this sandbox: cookie session + `web` client -> audio-only formats present (139/249/250/233/234); + `android` -> ONLY progressive format 18 (360p + 44k mp4a), so `bestaudio` matches nothing; + `tv` -> "page needs to be reloaded". Fix (commit 89b1243): extract_audio now walks a CONFIG LADDER — the first config that produces an audio file wins; format selector widened to `bestaudio/best[acodec!=none]/best` so progressive formats with embedded audio also match. Later (commit 9fa94f2): each config carries a player-client LIST (yt-dlp falls through the list on per-client exceptions) + job errors deduped (up to 4 distinct attempts). Then a full 22-cookie set passed the bot check from a clean IP but Render's Singapore egress began bot-checking EVERY client (web/android/ios/tv, cookies or not) even with valid cookies -> IP/ASN-level flag by YouTube (proven: identical cookies + web client work from the sandbox, fail from Render). Commits 99207b4: ladder reordered COOKIE-FIRST when cookies exist (cookie path also protects the IP from anonymous bot-check failures that cause the flagging) + render.yaml region singapore -> frankfurt for a fresh egress IP (blueprint region change may NOT auto-apply to an existing service — if the dashboard still shows singapore, user must change Region in Render dashboard Settings; that forces a redeploy on the new region). STATUS: if Frankfurt egress is also bot-blocked (possible ASN-level flag on Render free tier), the reliable options are: run coda-play on the user's own PC/Termux (residential IP — full pipeline verified there, README has the command) or the user's Oracle free VPS (fresh datacenter IP, may need the same cookie re-upload flow). Local sandbox E2E green throughout (default config, CC-BY Big Buck Bunny, 635s).
- 2026-09-24 — YouTube cookies feature: user hit "Sign in to confirm you're not a bot" on the Render instance (YouTube datacenter-IP block). Added: POST/GET/DELETE /api/cookies (multipart file 'cookies' or raw text, Netscape format, stored in data/cookies.txt — git-ignored, never in repo), yt-dlp picks up cookiefile when present, bot-block errors now append a "upload cookies in Settings" hint, UI Settings card (collapsible, cookie upload/remove/status), README troubleshooting section. Tests 10/10 (2 new cookie-flow tests). NOTE: a workspace snapshot reset dropped the COOKIES_FILE constant from server.py mid-turn (references survived, definition gone) — re-applied; lesson: after any reset, re-run flake8 before trusting the file. Local E2E verified with cookies present (CC-BY video -> mp3 done) then cookies deleted. ffmpeg re-downloaded for testing then removed again per user's cleanup order.
- 2026-09-24 — Render setup + workspace cleanup: added render.yaml (Blueprint: Docker runtime, free plan, singapore region, healthCheckPath /api/health) + detailed Render deploy section in README (free-tier reality: 15-min sleep + ephemeral disk = playlist resets on redeploy; permanent library => own machine or Oracle free VPS). Workspace cleaned per user order: deleted 15MB test mp3 + thumb, 80MB ffmpeg-static, logs, caches (playlist reset to empty). Kept: coda-play code (471KB, 23 files), SKILL.md, .ssh, .gitconfig. NOTE: local extraction in this sandbox now needs ffmpeg re-download (~5s from johnvansickle.com static build -> /home/user/ffmpeg-static + /home/user/bin symlinks) until the user wants local E2E again; Render deploy is unaffected (Dockerfile apt-installs ffmpeg).
- 2026-09-24 — CI GREEN: first CI run failed on all 3 Pythons at pytest — bare `pytest tests/ -q` leaves the repo root off sys.path (no tests/__init__.py) so `import server` failed in CI while local `python -m pytest` passed. Fixed ci.yml to `python -m pytest tests/ -q` (aligned with Makefile), commit 3130f2f, CI success on 3.11/3.12/3.13. Note: repo also has GitHub Pages enabled (auto, "pages build and deployment" success) — Pages only serves static files, it does NOT run the Flask app; for a live server deploy to Render free plan (per user rule: Render only).
- 2026-09-24 — DEPLOYED: pushed to github.com/haziqcodes/coda-play (new account chosen by user from the suggested names; key "coda-workspace-key" added there, commit author email switched to haziqcodes@users.noreply.github.com). Initial commit d4d0a08 (22 files) live on main. Design polish same day per user order ("single blue not compulsory — labs.google inspiration, match the audio product"): animated 3-bar equalizer (blue, pauses with playback), gradient depth on logo + play button (#1a73e8 → #0b57d0/#1765cc), warm accent #e8710a for the repeat badge; EQ in now-playing rows and player bar (body.paused / playerBar.playing class logic). Verified: pytest 8/8, flake8 clean, emoji-scan clean, curl /, /api/health, /api/playlist, /ai all 200.
- 2026-09-24 — Git ready: git init (main), initial commit 2dbb405 with full v1 (22 files, structured commit message incl. User asked / Contents / Verified), remote origin set to git@github.com:haziqcodes/coda-play.git. Snapshot-reset recovery earlier same day: .github/ + data/tracks + ffmpeg binary were wiped by a workspace snapshot reset and were rebuilt/re-downloaded; pip packages reinstalled; E2E re-verified after recovery (mp3 206 range, thumb 200). LESSON (matches class10 note): ALWAYS re-check remote/identity/ssh-perms/binaries before pushing after any reset.
- 2026-09-24 — Premium restyle + handoff system: light Google-labs theme (#f6f8fa bg, white cards, #1a73e8 accent, Inter/JetBrains Mono), all emojis replaced by inline SVG (play/pause/prev/next/repeat/grip/ext/del/note), hidden /ai handoff endpoint (noindex), ai/handoff.md created, /home/user/SKILL.md rebuilt from class10-learning-hub lib/handoff.ts rules, git identity Mohd Haziq <mohdhaziq-work@users.noreply.github.com> + /home/user/.ssh/config set, SSH key generated (ED25519, fingerprint SHA256:Cd8jYimCr8zLZzaXfGiwk3SVLNfqqEla31mFwNI/Mok). Verified: pytest 8/8, flake8 clean, emoji-scan of static/ empty, curl smoke all green.
- 2026-09-24 — Professional setup: MIT LICENSE, Makefile, Dockerfile + docker-compose, .editorconfig, tests/test_api.py (8 tests), .github CI (3 Python versions), Dependabot, issue/PR templates, CONTRIBUTING, CoC, README with badges.
- 2026-09-24 — v1 core: Flask + yt-dlp + ffmpeg pipeline, playlist API (rename/reorder/remove), single-audio player with repeat off/all/one, drag & drop, range streaming, thumbnails, progress jobs, dark-theme UI (later restyled). End-to-end verified with CC-BY Big Buck Bunny.
